# Settings
### Урок 10.


В прошлом уроке мы с вами написали постволл, в котором требовалось вводить имя, так как авторизацию не смогли впихнуть в один урок. Обязательно сделаем! Но пока давайте разберемся со следующим вопросом. Каждый раз вводить имя перед тем как отправить сообщение не очень то и удобно. Поэтому в этом уроке мы научимся сохранять разные значения в ПЗУ мобильного устройства и читать их. Заодно сделаем экран настроек и интересная фишка: разнообразим настройки нашего приложения в системных настройках. Погнали!


1. Для начала нам нужно сделать так, чтоб имя сохранялось при отправке сообщения. Сохранять как и читать мы будем при помощи объекта UserDefaults. Согласно документации это интерфейс базы данных пользователя, который работает по принципу ключ-значение. В нашем ChatViewController.swift создадим функцию viewDidAppear и напишем следующий код:

``` swift
override func viewDidAppear(_ animated: Bool) {
    nameField.text = UserDefaults.standard.string(forKey: "chatNameField")
}
```
Разберем. Думаю слова «nameField.text =» никого уже не пугают и мы понимаем что здесь идет присвоение значение текста нашему nameField`y. А вот чему мы его присваеваем уже интересней. UserDefaults — это тот самый интерфейс, о котором рассказал чуть ранее. В нем мы обращаемся к объекту standard, что будем работать с чем-то стандартным. Однако согласно документации это глобальный объект типа NSUserDefaults (приставка NS будет нам часто встречаться, откуда она взялась мы расскажем в одной из теории). Он будет доступен нам везде в приложении. И с его помощью мы записывать и считывать нужные нам данные. Строка chatNameField — это строковый идентификатор или ключ, по которому будет лежать наше значение. Можем выбрать любое, но так как таких ключей в приложений может появиться много в будущем, то лучше сразу назвать его интуитивно понятно.


2. Ну считать мы уже считали, теперь бы сохранить что-нибудь, чтоб было что считывать🤪. Сохранять будем при отправке сообщения, найдем функцию send. Напишем следующий код:

``` swift 
UserDefaults.standard.set(self.nameField.text, forKey: "chatNameField")
```

Так же как и при чтении мы обращаемся к standard объекта UserDefaults, но на этот раз нам надо записать и не считать. Делаем это при помощи функции set. Указываем значение, которое нужно сохранить, и вторым параметром указываем в какой ключ его записать. Вроде все всё интуитивно понятно и здесь. Сделаю маленькую ремарку. Искушенные в программировании спросят, где же synchronize? А вот нигде;), если мы пройдем на объявление этой функции, то увидим что эппл уже написали: «synchronize is deprecated and will be marked with the NS_DEPRECATED macro in a future release», то есть в использовании оной уже нет необходимости и без неё будет всё работать;).

3. Давайте запустим наше приложение, введем имя, нажмем на кнопку отправки и выключим приложение. Запускаем снова и видим что наше поле имя уже заполнено! Удобно. Так плавно мы подошли к теме настроек нашего приложения, которое должны сохраняться. А значит самое время заняться экраном настроек. Хотя принцип Вам уже понятен. Мы будем создавать элементы, на viewDidAppear будем считывать в элементы их значения из UserDefaults.standard, а при изменении мы будем их сохранять при помощи set. Только ключи будут отличаться. Но в конце мы вам расскажем как вставить в настройки нашего приложения в системные настройки, вот сюда. Так что не отключайтесь;)


4. Список настроек у нас будет скромный, потому что потом мы собираемся вынести его в системные настройки, а там есть определенные ограничения. Так что перейдем в main.storyboard и там выберем экран настроек. Удалим существующие элементы, не забудем Создадим элементы для следующего списка настроек:
- Приоритетная тема;
 - Имя (сюда продублируем наше имя);
 - Уведомлять о новостях сообщества;
 - Количество новостей на странице;
На создании элементов и выставлению им констрейтов заострять внимание не будем, покажем ускорено и результат, так как я думаю все уже знают как это делается. Но рассмотрим один новый компонент для разнообразия: UIPickerView. Работает он так же как и UITableView в плане что использовать будем его delegate и dataSource.


5. Создадим переменную themes, она будет массивом строк, в ней будут хранится темы на выбор, первым пунктом пойдет пункт «Все»: 

``` swift 
var themes = ["Все", "Swift", "Android", "Сервер"]
```


Добавим UIPickerViewDelegate и UIPickerViewDataSource в наш ViewController. Появившуюся ошибку разрешаем кнопкой fix. И вписываем код в эти функции следующим образом:

``` swift
func numberOfComponents(in pickerView: UIPickerView) -> Int {
    return 1
}

func pickerView(_ pickerView: UIPickerView, numberOfRowsInComponent component: Int) -> Int {
    return self.themes.count
}
```

Столбцов в нашем пикере будет 1, а количество строк будет столько, сколько элементов в нашем массиве themes. Всё просто. Для нашего пикера нужно еще две функции, одна возвращает какую строку выводить в пикер в определенной позиции: 

``` swift
func pickerView(_ pickerView: UIPickerView, titleForRow row: Int, forComponent component: Int) -> String? {
    return self.themes[row]
}
```

Вторая функция будет вызываться когда пользователь будет выбирать определенную строку:

``` swift
func pickerView(_ pickerView: UIPickerView, didSelectRow row: Int, inComponent component: Int) {
    UserDefaults.standard.set(self.themes[row], forKey: "favoriteTheme")
    self.view.endEditing(true)
}
```

Сохраняем выбранную строку с помощью UserDefaults в ключ favoriteTheme. И выключим редактирование, это задел нам нужен для нашего поля для имени.

6. С помощью того же Connection Inspector`a создадим связи элементов:
 - themesPicker — для пикера тем;
 - nameField — для текстового поля под наше имя;
 - notificationSwitch — для переключателя уведомлений;
 - countPerPage — для слайдера количества новостей на странице;
 - countPerPageLabel — для label, который идет справа от заголовка «Количество новостей на странице»;


7. Еще с помощью того же Connection Inspector`a создадим три функции:
 - nameEditEnd — связь Editing Did End нашего поля для имени. В нем просто сохраним имя для чата. Ключ его мы уже знаем: chatNameField
 - notificationChanged — связь Value Changed нашего свитчера «Присылать уведомления». Сохраним значение isOn в ключ «notificationMe» и выключим редактирование для нашего вью, так же как при выборе темы.
 - countPerPage — связь Value Changed нашего слайдера, который отвечает за количество новостей на страницу. Прировняем значение нашего слайдера к переменной ц, преобразовав значение в целочисленный Int, так как значение слайдера — дробное. И сохраним значние этой переменной в ключ countPerPage. Так же не забудем вывести значение в countPerPageLabel и как и в прошлой функции выключим редактирование.

``` swift
@IBAction func nameEditEnd(_ sender: Any) {
    UserDefaults.standard.set(self.nameField.text, forKey: "chatNameField")
}

@IBAction func notificationChanged(_ sender: Any) {
    UserDefaults.standard.set(self.notificationSwitch.isOn, forKey: "notificateMe")
    self.view.endEditing(true)
}

@IBAction func countPerPageChanged(_ sender: Any) {
    let c: Int = Int(self.countPerPage.value)
    UserDefaults.standard.set(c, forKey: "countPerPage")
    self.countPerPageLabel.text = c.description
    self.view.endEditing(true)
}
```

8. Осталось только выставить ранее сохраненные значения (если таковые были) в наши элементы. Любимую тему — в пикер, Имя — в текстовое поле и остальные. Делать мы будем это по функции viewDidAppear. И выглядит она так:

``` swift

override func viewDidAppear(_ animated: Bool) {
    let theme = UserDefaults.standard.string(forKey: "favoriteTheme") ?? ""
    let i = self.themes.firstIndex(of: theme) ?? 0
    self.themesPicker.selectRow(i, inComponent: 0, animated: true)
    self.nameField.text = UserDefaults.standard.string(forKey: "chatNameField")
    let c: Int = UserDefaults.standard.integer(forKey: «countPerPage")
    self.countPerPage.setValue(Float(c), animated: true)
    self.countPerPageLabel.text = c.description
    let n: Bool = UserDefaults.standard.bool(forKey: «notificateMe")
    self.notificationSwitch.setOn(n, animated: true)
}
```

 - Считаем тему из ключа favoriteTheme и если такой ключ не был сохранен, то вернем пустую строку с помощью двойного знака вопроса.
 - Далее найдем сохраненную тему в нашем массиве themes. И если мы не нашли такого элемента, то вернем индекс 0 с помощью тех же волшебных задвоенных вопросов. А индекс 0 — это первая тема «Все».
 - Ну и пусть анимировано выставим нужную строку в нашем пикере.
 - Загрузим имя в нужное поле
 - Считаем ключ countPerPage.
 - Выставим это значение нашему слайдеру countPerPage, так же анимейтед тру
 - Это же число выведем в countPerPageLabel
 - считаем ключ notificationMe
 - выставим его в наш notificationSwitch, снова анимировано.
Теперь можем запустить и поиграться с сохраняющимися настройками: открываем приложение, меняем настройки переходим в чат и назад, закрываем приложение, открываем снова — все остается на своих местах! Круто выглядит, просто делается;)


9. Ну и напоследок вкусненькое. Как же вынести эти настройки в системные настройки иоса? Создаем новый файл: жмем File -> New -> File… Листаем вниз до категории Resource, ищем иконку белого Лего блока с названием Settings Bundle, жмем Next. Оставляем имя Settings и сохраняем. Раскрываем созданный файл. Папку en.lproj пока оставляем в покое, скажу что en в названии означает ENglish и это задел для локализации приложения, то есть с переводом приложения на разные языки. В этом уроке мы не успеем рассказать про это, но разберем это в отдельном уроке. Пока все пишем по-русски и не паримся;) И так нас интересует файл Root.plist. В разделе iPhone Settings Schema, внутри находим и открываем Preference Items. Видим что Xcode любезно создал для нас 4 пункта меню, давайте заменим их на наши.
 - Первый пункт у нас будет с возможностью выбора темы. Здесь выбираем Multi Value. В строке Title пишем «Интересующие темы». Нажимаем около строки Title + и выбираем Identifier и вписываем наш ключ, в который мы вписываем значение нашего PickerView, то есть favoriteTheme. Жмем еще раз плюсик на Title и добавляем раздел Titles. Разворачиваем его и жмем четыре раза плюс и сверху вниз вписываем наши значения: Все, Swift, Android и Сервер. Повторяем операцию, но добавляем теперь Values и снова 4 раза плюс и снова: Все, Swift, Android и Сервер. И последний параметр добавим Default value, то есть значение по умолчанию, равный Все.
 - Второй пункт: Выбираем его тип Text Field, в Title пишем Имя, а в Identifier вписываем наш ключ для поля чата: chatNameField
 - Третий: тип выбираем Toggle Switch. Думаю вы уже принцип поняли заполняем так: Identifier = notificateMe, Title = Присылать уведомление, Default Value = YES.
 - Далее нам надо добавить заголовок для нашего слайдера. Создадим тип Group и дадим Title = «Кол-во новостей на странице»
 - Ну и последний элемент — наш слайдер. Тип Slider, Identifier = countPerPage. Default Value = 5, Minimum Value = 5, Maximum Value = 30.


10. Ну вот и всё давайте запустим приложение и поиграемся с настройками внутри системы и внутри нашего приложения, увидим что параметры меняются.


**Сейчас попробуйте по памяти все повторить, желательно 2-3 раза.**






